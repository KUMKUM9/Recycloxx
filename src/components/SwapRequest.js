import React from "react";

const SwapRequest = () => {
  return (
    <div>
      {/* Modal for requesting a swap */}
      <h2>Swap Request</h2>
      <p>Request to swap this item with another listing.</p>
    </div>
  );
};

export default SwapRequest;
